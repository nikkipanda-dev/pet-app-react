import { LogIn } from "./LogIn/LogIn";

export const AuthIdx = ({ authContext }) => {
    console.log('authContext: ', authContext);

    return (
        <div>
            I am authhhh
        </div>
    )
};
